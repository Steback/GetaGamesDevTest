using UnityEngine;
using UnityEngine.Serialization;

namespace Gameplay
{
    public class Obstacle : MonoBehaviour
    {
        [SerializeField] public int penaltyTime;
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
