using UnityEngine;

namespace Gameplay
{
    public class JumpBoost : MonoBehaviour
    {
        [SerializeField] public float distanceJump;
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
